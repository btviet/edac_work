
Create an .env file
and set LOCAL_WORKING_DIR to the path of where the repository is cloned and
DATA_DIR to be the path to the data direcotry outside of LOCAL_WORKING_DIR.
Inside of DATA_DIR there should be a folder "raw_data" with raw EDAC data and sunspot number.
